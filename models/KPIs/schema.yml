
version: 2

models:
    - name: DAU
      description: "Daily Actyive Users (DAU) per day by product"
      columns:
          - name: event_date
            tests:
                - unique
                - not_null

